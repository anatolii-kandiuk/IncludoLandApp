# ===== Django core =====
# Generate a strong random secret for production.
SECRET_KEY=change-me

# For Railway production set DEBUG=False
DEBUG=False

# Comma-separated hosts. Railway provides a public domain like: <project>.up.railway.app
ALLOWED_HOSTS=localhost,127.0.0.1,<your-railway-domain>

# Comma-separated origins (with scheme). Required for POST forms/login over HTTPS.
CSRF_TRUSTED_ORIGINS=https://<your-railway-domain>

# If you use API calls from a frontend domain, add it here.
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Redirect to HTTPS behind proxy (Railway) and secure cookies.
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

# ===== Database (Railway recommended) =====
# Preferred: set DATABASE_URL from Railway Postgres plugin.
DATABASE_URL=

# Fallback (local/docker-compose) if DATABASE_URL is not set.
DB_NAME=includoland_db
DB_USER=includoland_user
DB_PASSWORD=includoland_password
DB_HOST=db
DB_PORT=5432

# ===== Gunicorn (optional tuning) =====
GUNICORN_WORKERS=2
GUNICORN_THREADS=4
GUNICORN_TIMEOUT=120
